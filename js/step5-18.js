(function(){"use strict";var SAVE_KEY="bugcapture_save_v1";var result=document.getElementById("soldierTimingResult");var retryButton=document.getElementById("soldierRetryButton");var valueEl=document.getElementById("soldierSensitivityValue");var fillEl=document.getElementById("soldierSensitivityFill");var noteEl=document.getElementById("soldierSensitivityNote");var resetButton=document.getElementById("resetButton");var handled="";function load(){try{return JSON.parse(localStorage.getItem(SAVE_KEY)||"null")||{};}catch(e){return {};}}function encounter(data){data.world=data.world||{};data.world.encounters=data.world.encounters||{};data.world.encounters.soldier_ant=data.world.encounters.soldier_ant||{};return data.world.encounters.soldier_ant;}function saveSensitivity(value){var data=load();encounter(data).sensitivity=value;localStorage.setItem(SAVE_KEY,JSON.stringify(data));render(value);}function current(){var data=load();return Math.max(0,Math.min(100,Number(encounter(data).sensitivity||0)));}function render(value){value=Math.max(0,Math.min(100,Number(value||0)));valueEl.textContent=value+"%";fillEl.style.width=value+"%";if(value<25)noteEl.textContent="장군개미는 아직 침착합니다.";else if(value<50)noteEl.textContent="장군개미가 주변을 경계하기 시작했습니다.";else if(value<75)noteEl.textContent="장군개미가 매우 예민해졌습니다.";else noteEl.textContent="장군개미가 강하게 경계하고 있습니다.";}var observer=new MutationObserver(function(){var text=result.textContent.trim();if(!text||text===handled)return;handled=text;if(text==="MISS"){var next=Math.min(100,current()+25);saveSensitivity(next);noteEl.textContent="채집 실패로 민감도가 25% 상승했습니다. 현재 "+next+"%";}else if(text.indexOf("포획 성공")>=0){saveSensitivity(0);}});observer.observe(result,{childList:true,characterData:true,subtree:true});retryButton.addEventListener("click",function(){handled="";render(current());});if(resetButton){resetButton.addEventListener("click",function(){window.setTimeout(function(){saveSensitivity(0);handled="";},0);});}window.addEventListener("pageshow",function(){render(current());});render(current());})();